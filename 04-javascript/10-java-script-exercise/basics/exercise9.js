/**
 * find odd and even from a given range
 */
for (let i = 1; i <= 101; i++) {
  if (i % 2 == 0) {
    console.log(`${i} is even Number`);
  } else {
    console.log(`${i} is odd Number`);
  }
}
